# Test data

Cross-package fixtures and Windows integration-test support belong here. Package-local unit fixtures should remain beside the package that owns them.

The Windows 10 stage 1 acceptance procedure is documented in
[`WINDOWS10_STAGE1.md`](WINDOWS10_STAGE1.md). Its PowerShell smoke test writes
reports and screenshots under the ignored `dist/` directory.
